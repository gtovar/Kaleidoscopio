
<div class="center hero-unit">
  <h1>Ver contactos</h1>

  <h2>
    Es una pagina ver un
    <a href="#">Contacto</a>
    version de Desarrollo
  </h2>
  <%= link_to "Crear contacto", new_contact_path, class: "btn btn-large btn-primary" %>
</div>

<table>
  <tr>
    <th><%= Contact.human_attribute_name(:name) %></th>
    <th><%= Contact.human_attribute_name(:email) %></th>
    <th><%= Contact.human_attribute_name(:phone) %></th>
    <th><%= Contact.human_attribute_name(:subjects) %></th>
    <th><%= Contact.human_attribute_name(:message) %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @contacts.each do |contact| %>
  <tr>
    <td><%= contact.name %></td>
    <td><%= contact.email %></td>
    <td><%= contact.phone %></td>
    <td><%= contact.subjects %></td>
    <td><%= truncate (contact.message) %></td>
    <td><%= link_to 'Show', contact %></td>
    <td><%= link_to 'Edit', edit_contact_path(contact) %></td>
    <td><%= link_to 'Destroy', contact, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />
